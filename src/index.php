<?php
// Comment out the following line once your database connection is configured
$noDB = true;

// Change the following to reflect which environment you are using
$env = "dev";

// Load the application bootstrap file
include_once 'app/bootstrap.php';
